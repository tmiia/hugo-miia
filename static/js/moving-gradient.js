// const colored_text = document.getElementById("colored-name");

// colored_text.addEventListener("mousemove", (e)=>{
//   moveGradient(e.clientX, e.clientY);
// })

// function moveGradient(posX, posY) {
//   let = grandientX = window.getComputedStyle(colored_text).getPropertyValue('background-position-x');
//   let = grandientY = window.getComputedStyle(colored_text).getPropertyValue('background-position-Y');

//   colored_text.style.backgroundPositionX = ((parseInt(grandientX) + (posX/0.07)) / 100).toString()+"%";
//   colored_text.style.backgroundPositionY = ((parseInt(grandientY) + (posY/0.07)) / 100).toString()+"%";
// }
